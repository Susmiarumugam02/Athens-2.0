# QUICK_REF_FRONTEND
Stack: React 18 + TypeScript
UI: keep SAP-Python parity patterns already used (layout/container/sidebar/header).
Rules:
- Reuse existing components/utilities; do not introduce new UI libs unless told.
- Prefer minimal prop/API changes; keep routes stable.
Deliverable: unified diffs only; max files requested.
